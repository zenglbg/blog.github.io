{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "deploy": "yarn && yarn build && node .output/server/index.mjs"
  },
  "devDependencies": {
    "@element-plus/nuxt": "^1.0.3",
    "nuxt": "^3.3.3",
    "sass": "^1.58.3"
  },
  "dependencies": {
    "element-plus": "^2.2.32"
  }
}
